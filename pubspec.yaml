name: appsgco
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.9.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^2.25.0
  cloud_firestore: ^4.15.0
  geolocator: ^10.1.0
  camera: ^0.10.5+5    # Hoặc phiên bản mới nuget
  path_provider: ^2.1.5
    # Dùng nếu cần lưu ảnhversion:
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/sgco_logo.png
